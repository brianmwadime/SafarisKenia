<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent" 
    android:layout_height="fill_parent"
    android:orientation="horizontal"
    android:background="#ffffff">
<RelativeLayout
 		android:id="@+id/package_r_title"
    	android:orientation="horizontal"
		android:layout_width="fill_parent" 
		android:layout_height="wrap_content"
    	android:background="#004429"
		android:paddingTop="5dip"
		android:paddingBottom="5dip" >
		<TextView android:id="@+id/package_det_title" android:layout_width="fill_parent"
			android:layout_height="wrap_content" android:text="@string/package_det_title" android:gravity="center_vertical" 
			android:textSize="13sp" android:textColor="#FFFFFF" android:textStyle="bold" android:layout_centerVertical="true"
			android:padding="3dip"
			/>
		</RelativeLayout>

	    <ListView
	        android:id="@+id/package_det"
	        android:layout_width="fill_parent"
	        android:layout_height="fill_parent"
	        android:layout_alignParentLeft="true"
	        android:layout_below="@+id/package_r_title" >

	    </ListView>

</RelativeLayout>